# Basic Eve Knowledge Example

This example shows a support-style Eve agent answering from committed
`agent/knowledge/` docs with citations.

From the repo root:

```bash
npm run demo:support
```

The demo indexes the example knowledge base and prints a cited answer using
the same search runtime that powers the generated `search_knowledge` Eve tool.
